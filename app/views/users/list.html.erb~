<h1>List of post's user</h1>

<%= @list.each do |lis| %>
<p>
<%= lis.title %>
<%= lis.text %>
<%= Post.where(user_id:params[:id]).title %>
</p>
<% end %>

<br><br>
<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>


